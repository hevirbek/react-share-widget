import React from 'react';

interface TwitterButtonProps {
    text: string;
}

const TwitterButton = (props: TwitterButtonProps) => {
    return <button>Twitter Button</button>
}

export default TwitterButton